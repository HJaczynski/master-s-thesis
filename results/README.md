# Results

Generated raw result dumps are ignored by Git. Keep only curated tables,
templates, and final figures here.

Create the result CSV template with:

```powershell
.\venv\Scripts\python.exe -m cv_model_benchmark.cli results-template
```
