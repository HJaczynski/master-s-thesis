# Python dependencies
torch>=2.0.0
torchvision>=0.15.0
pandas>=1.5.0
seaborn>=0.12.0
matplotlib>=3.5.0
numpy>=1.21.0
psutil>=5.9.0
pyyaml>=6.0
pycocotools>=2.0.7
tqdm>=4.66.0

# Detector/tooling dependencies
rfdetr>=1.6.0
ultralytics>=8.4.0
onnx>=1.16.0

# Optional export/runtime dependency. TensorRT support may require a separate
# NVIDIA install matching the local CUDA and driver stack.
# onnxruntime-gpu>=1.18.0

# Development dependencies (optional)
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0
jupyter>=1.0.0
